# Desktop app deps
customtkinter>=5.2.0
Pillow>=10.0.0
openai>=1.0.0
requests>=2.31.0
keyring>=24.0.0
tkinterdnd2>=0.3.0
pyinstaller>=6.0.0

# MCP server deps (also listed slim in requirements-server.txt for deployment)
fastmcp>=3.0.0   # verified booting on 3.4.4
uvicorn>=0.30.0
